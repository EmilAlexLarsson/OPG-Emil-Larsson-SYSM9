<Window x:Class="CookMaster.View.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.View"
        mc:Ignorable="d"
        Title="RecipeListWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button Content="info" Command="{Binding InfoCommand}" HorizontalAlignment="Left"/>
            <Label Content="Recipes" FontSize="50" FontStyle="Italic"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <TextBox Width="100" Margin="5" Text="{Binding Search ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Filter" Command="{Binding FilterCommand}" Margin="5"/>
            <Button Content="Sort by newest" Margin="5" Command="{Binding SortCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1">
            
            <!--<TextBlock Text="{Binding Source={StaticResource UserManager}, Path=LoggedIn.Username}"/>-->
            <TextBlock HorizontalAlignment="Center" Margin="5" Text="{Binding UserManager.LoggedIn.Username}"/>
            <Button Content="User" Command="{Binding OpenUserCommand}" Margin="5" Width="100"/>
            <Button Content="Log out" Command="{Binding SignOutCommand}" Margin="5" Width="100"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Recipes}" SelectedItem="{Binding SelectedRecipe, Mode=TwoWay}" DisplayMemberPath="Title"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <Button Content="Add Recipe" Margin="5" Width="100" Command="{Binding OpenAddRecipeCommand}"/>
            <Button Content="Details" Command="{Binding DetailsCommand}" Margin="5" Width="100"/>
            <Button Content="Remove" Command="{Binding RemoveCommand}" Margin="5" Width="100"/>

        </StackPanel>
        
    </Grid>
</Window>
