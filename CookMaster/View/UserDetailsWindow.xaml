<Window x:Class="CookMaster.View.UserDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.View"
        mc:Ignorable="d"
        Title="UserDetailsWindow" Height="450" Width="300">
    <StackPanel>
        
        <StackPanel Orientation="Horizontal">
            <Label Content="Username:" Margin="5"/>
            <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="New Password" Margin="5" />
            <TextBox Text="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5" Width="150"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" >
            <Label Content="Confirm Password" Margin="5"/>
            <TextBox Text="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5" Width="130"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Country:" Margin="5"/>
            <ComboBox Width="200" ItemsSource="{Binding UserManager.Countries}" SelectedItem="{Binding SelectedCountry, Mode=TwoWay}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Save" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </StackPanel>
</Window>
